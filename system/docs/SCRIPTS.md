# Документация скриптов plan_expo

Подробное описание всех скриптов автоматизации plan_expo с примерами использования, параметрами и внутренним устройством.

## Содержание

1. [validate_goals.py](#validate_goalspy)
2. [auto_update_metrics.py](#auto_update_metricspy)
3. [track_habit_streaks.py](#track_habit_streakspy)
4. [generate_daily_dashboard.py](#generate_daily_dashboardpy)
5. [generate_weekly_dashboard.py](#generate_weekly_dashboardpy)
6. [git_auto_commit.py](#git_auto_commitpy)
7. [schedule_manager.py](#schedule_managerpy)
8. [notification_system.py](#notification_systempy)

---

## validate_goals.py

### Назначение

Валидация структуры файлов целей и рефлексий. Проверяет целостность данных и автоматически исправляет распространенные ошибки.

### Использование

```bash
# Только проверка (без изменений)
python3 scripts/validate_goals.py

# С автоматическим исправлением
python3 scripts/validate_goals.py --fix
```

### Параметры

```
--fix    Автоматически исправить найденные проблемы
```

### Что проверяется

#### 1. Структура файлов целей

Обязательные секции:
- `# СТРАТЕГИЧЕСКИЙ УРОВЕНЬ`
- `# ТАКТИЧЕСКИЙ УРОВЕНЬ`
- `# ОПЕРАЦИОННЫЙ УРОВЕНЬ`
- `## ИСТОРИЯ ИЗМЕНЕНИЙ`

Обязательные метаданные:
- `**Статус:**` (active|paused|cancelled|completed)
- `**Дата создания:**` (формат YYYY-MM-DD)

Опциональные метаданные:
- `**Прогресс:**` (0-100%)
- `**Дата завершения:**` (формат YYYY-MM-DD)

#### 2. Структура директорий рефлексий

Правильная структура:
```
reflections/daily/
├── YYYY/
│   ├── MM/
│   │   ├── YYYY-MM-DD.md
│   │   └── ...
```

Неправильная структура (будет исправлена с --fix):
```
reflections/daily/
├── YYYY-MM-DD.md  ❌
```

### Выходные данные

#### Отчет валидации

Сохраняется в: `dashboards/validation/YYYY-MM-DD_validation_report.md`

Структура отчета:
```markdown
# Отчет валидации: YYYY-MM-DD

## Сводка
- Проверено целей: N
- Критических ошибок: N
- Предупреждений: N

## Детали

### Цель: goal_name
- ✅ Структура корректна
- ⚠️ Отсутствует секция X
- ❌ Некорректный формат даты

### Рефлексии
- Исправлено файлов: N
- Ошибок миграции: N
```

### Exit коды

```
0  - Валидация прошла успешно (или ошибки исправлены с --fix)
1  - Критические ошибки (требуется ручное вмешательство)
```

### Примеры

```bash
# Проверка перед коммитом
python3 scripts/validate_goals.py
if [ $? -eq 0 ]; then
    git add .
    git commit -m "Update goals"
fi

# Автоматическое исправление структуры рефлексий
python3 scripts/validate_goals.py --fix

# Просмотр отчета
cat dashboards/validation/$(date +%Y-%m-%d)_validation_report.md
```

---

## auto_update_metrics.py

### Назначение

Автоматическое обновление метрик в файлах целей на основе данных из рефлексий.

### Использование

```bash
# Обновить метрики за сегодня
python3 scripts/auto_update_metrics.py

# За конкретную дату
python3 scripts/auto_update_metrics.py --date 2025-12-21

# За период (неделя, месяц)
python3 scripts/auto_update_metrics.py --period week
python3 scripts/auto_update_metrics.py --period month
```

### Параметры

```
--date YYYY-MM-DD    Дата рефлексии для обработки (по умолчанию: сегодня)
--period PERIOD      Период: week, month (обработает все дни периода)
```

### Что обновляется

#### 1. Identity Evidence List

Подсчитывает количество выполненных доказательств идентичности:

```markdown
## Identity Evidence List
- [x] Доказательство 1  ← Учитывается
- [ ] Доказательство 2  ← Не учитывается
- [x] Доказательство 3  ← Учитывается

**Выполнено:** 2/3 доказательств
```

#### 2. OKR/SMART Tracker

Обновляет процент выполнения на основе прогресса операций:

```markdown
## OKR/SMART Tracker
**Прогресс:** 45% → 47%  ← Обновляется
```

#### 3. Daily Habits Tracker

Обновляет streaks и проценты выполнения привычек:

```markdown
## Daily Habits Tracker
| Привычка | Streak | % за 7 дней |
|----------|--------|-------------|
| Habit 1  | 5      | 85%         |  ← Обновляется
```

#### 4. История изменений

Добавляет запись с типом [PROGRESS]:

```markdown
## ИСТОРИЯ ИЗМЕНЕНИЙ

### 2025-12-21 23:30 [PROGRESS]
Автоматическое обновление метрик:
- Операции: 75% выполнено
- Тактика: 60% выполнено
- Доказательств: 3 добавлено
```

### Логирование

Детальные логи сохраняются в: `logs/metrics_update_YYYY-MM-DD.log`

Формат лога:
```
2025-12-21 21:05:30,123 - INFO - Начало обновления метрик
2025-12-21 21:05:30,124 - INFO - Обработка рефлексии за 2025-12-21
2025-12-21 21:05:30,125 - INFO - Найдено активных целей: 3
2025-12-21 21:05:30,130 - INFO - Обновлено целей: 2/3
2025-12-21 21:05:30,131 - INFO - Завершено
```

### Примеры

```bash
# Обновить метрики после заполнения рефлексии
python3 scripts/auto_update_metrics.py

# Пересчитать метрики за прошлую неделю
python3 scripts/auto_update_metrics.py --period week

# Проверить логи
tail -20 logs/metrics_update_$(date +%Y-%m-%d).log
```

---

## track_habit_streaks.py

### Назначение

Отслеживание непрерывных цепочек (streaks) выполнения привычек из активных целей.

### Использование

```bash
python3 scripts/track_habit_streaks.py
```

### Что анализируется

#### 1. Implementation Intentions

Извлекает из целей:
```markdown
- ЕСЛИ утром встаю → ТО делаю зарядку
- ЕСЛИ 9:00 → ТО пишу connection request
```

#### 2. Tiny Habits

Извлекает из целей:
```markdown
- После утреннего кофе → connection request
- После обеда → 15 мин чтения
```

### Вычисляемые метрики

Для каждой привычки:

1. **Текущий streak** - количество дней подряд с момента последнего выполнения
2. **Максимальный streak** - лучшая серия за все время
3. **Процент выполнения:**
   - За 7 дней
   - За 30 дней
   - За все время (до 90 дней назад)
4. **Средняя частота** - сколько раз в неделю в среднем
5. **Паттерны:**
   - Лучшие дни недели (>50% выполнение)
   - Худшие дни недели (<50% выполнение)
   - Статистика по каждому дню недели

### Выходные данные

#### JSON файл

Сохраняется в: `dashboards/streaks/streaks_data.json`

Формат:
```json
{
  "generated_at": "2025-12-21T21:10:00",
  "habits": [
    {
      "name": "После утреннего кофе → connection request",
      "type": "tiny_habit",
      "goal": "goal_2025_12_21_income_1m_monthly",
      "current_streak": 7,
      "max_streak": 10,
      "completion_rate_7d": 100.0,
      "completion_rate_30d": 85.5,
      "completion_rate_all": 78.9,
      "avg_per_week": 6.2,
      "best_days": ["Monday", "Tuesday"],
      "worst_days": ["Saturday"],
      "day_stats": {
        "Monday": 95.2,
        "Tuesday": 90.0,
        "Saturday": 45.5
      }
    }
  ]
}
```

#### Markdown отчет

Сохраняется в: `dashboards/streaks/YYYY-MM-DD_streaks.md`

Структура:
```markdown
# Отчет по Streaks: YYYY-MM-DD

## Топ-5 стабильных привычек
1. 🔥 "Привычка 1..." - Streak: 14 дней (100%)
2. 🔥 "Привычка 2..." - Streak: 10 дней (95%)

## Требуют внимания
1. ⚠️ "Привычка 3..." - Streak: 0 дней (45%)
   - Рекомендация: Попробуйте изменить триггер

## Паттерны
- Лучший день недели: Monday (92% выполнение)
- Худший день: Saturday (58% выполнение)
```

### Примеры

```bash
# Отслеживание streaks
python3 scripts/track_habit_streaks.py

# Просмотр JSON данных
cat dashboards/streaks/streaks_data.json | jq .

# Просмотр отчета
cat dashboards/streaks/$(date +%Y-%m-%d)_streaks.md
```

---

## generate_daily_dashboard.py

### Назначение

Генерация интерактивного HTML дашборда с визуализацией дневного прогресса.

### Использование

```bash
# Дашборд за сегодня
python3 scripts/generate_daily_dashboard.py

# За конкретную дату
python3 scripts/generate_daily_dashboard.py --date 2025-12-21
```

### Параметры

```
--date YYYY-MM-DD    Дата для генерации (по умолчанию: сегодня)
```

### Содержимое дашборда

#### 1. Header

- Дата
- Текущий streak (крупно с эмодзи 🔥)
- Общая оценка дня (X/10)

#### 2. Метрики (Cards)

Три карточки с прогрессом:

```
┌──────────────────────┐
│ Операции             │
│ ████████░░ 80%      │
│ Зеленый (≥80%)      │
└──────────────────────┘

┌──────────────────────┐
│ Тактика              │
│ ██████░░░░ 65%      │
│ Желтый (60-79%)     │
└──────────────────────┘

┌──────────────────────┐
│ Доказательств        │
│ 5                    │
│ (за день)            │
└──────────────────────┘
```

Цветовая схема:
- 🟢 Зеленый: ≥80%
- 🟡 Желтый: 60-79%
- 🔴 Красный: <60%

#### 3. График трендов (7 дней)

SVG график с двумя линиями:
- 🔵 Операции (синий)
- 🟣 Тактика (фиолетовый)

С осями, сеткой и легендой.

#### 4. Топ-5 привычек

Таблица с:
- Название привычки
- Текущий streak
- Процент за 7 дней
- Визуальная индикация (🔥 если streak ≥7)

#### 5. Оценки дня

- Энергия
- Мотивация
- Фокус

### Выходные файлы

```
dashboards/daily/
├── 2025-12-21.html    # Интерактивный HTML
└── 2025-12-21.md      # Текстовая версия
```

### HTML особенности

- **Встроенный CSS** - нет внешних зависимостей
- **SVG графики** - нативные, без библиотек
- **Responsive дизайн** - адаптируется под размер экрана
- **macOS стиль** - цвета и шрифты из macOS Big Sur+

### Примеры

```bash
# Создать дашборд и открыть в браузере
python3 scripts/generate_daily_dashboard.py
open dashboards/daily/$(date +%Y-%m-%d).html

# Создать дашборд за вчера
python3 scripts/generate_daily_dashboard.py --date $(date -v-1d +%Y-%m-%d)

# Просмотр markdown версии
cat dashboards/daily/$(date +%Y-%m-%d).md
```

---

## generate_weekly_dashboard.py

### Назначение

Генерация недельного дашборда с аналитикой, сравнениями и инсайтами.

### Использование

```bash
# Дашборд за текущую неделю
python3 scripts/generate_weekly_dashboard.py

# За конкретную неделю
python3 scripts/generate_weekly_dashboard.py --week 50 --year 2025
```

### Параметры

```
--week WW      Номер недели (ISO неделя)
--year YYYY    Год
```

### Содержимое дашборда

#### 1. Header

- Период недели (Пн - Вс)
- Номер недели и год

#### 2. Сравнительная таблица

```markdown
| Метрика              | Текущая | Предыдущая | Изменение |
|----------------------|---------|------------|-----------|
| Средний % операций   | 85%     | 78%        | ↑ 7%      |
| Средний % тактики    | 72%     | 70%        | ↑ 2%      |
| Доказательств/день   | 4.2     | 3.8        | ↑ 0.4     |
```

Стрелки:
- ↑ - улучшение
- ↓ - ухудшение
- → - без изменений

#### 3. График выполнения

Bar chart (SVG) с производительностью по дням недели:
```
Пн ████████░ 85%
Вт ██████░░░ 72%
Ср ███████░░ 80%
Чт ████████░ 88%
Пт ██████░░░ 65%
Сб ████░░░░░ 45%
Вс █████░░░░ 58%
```

#### 4. Топ-3 достижения

Автоматически выявляет:
- Самый высокий процент операций
- Лучший день недели
- Максимальный streak достигнут

#### 5. Области для улучшения

Выявляет:
- Дни с низкой производительностью (<60%)
- Привычки с нулевым streak
- Паттерны неудач

#### 6. Инсайты и рекомендации

Генерирует автоматические рекомендации:
```markdown
### Инсайты
- Стабильная производительность в будни (80% в среднем)
- Спад в выходные (51% в среднем)

### Рекомендации
- Рассмотрите упрощение привычек на выходные
- Фокус на улучшении Saturday (45%)
```

### Выходные файлы

```
dashboards/weekly/
├── week_51_2025.html    # Интерактивный HTML
└── week_51_2025.md      # Текстовая версия
```

### Примеры

```bash
# Создать недельный дашборд
python3 scripts/generate_weekly_dashboard.py
open dashboards/weekly/week_$(date +%V)_$(date +%Y).html

# За прошлую неделю
python3 scripts/generate_weekly_dashboard.py --week 50 --year 2025

# Просмотр markdown
cat dashboards/weekly/week_$(date +%V)_$(date +%Y).md
```

---

## git_auto_commit.py

### Назначение

Автоматическое создание Git коммитов для рефлексий, дашбордов и обновлений целей.

### Использование

```bash
python3 scripts/git_auto_commit.py
```

### Конфигурация

Файл: `config/git_auto_commit.yaml`

```yaml
git:
  auto_commit: true         # Включить/отключить
  auto_push: false          # ОСТОРОЖНО: auto-push
  include_files:            # Фильтр файлов
    - "reflections/"
    - "dashboards/"
    - "goals/"
  templates:                # Шаблоны сообщений
    daily_reflection: "📝 Daily reflection: {date}"
    daily_dashboard: "📊 Daily dashboard: {date}"
    weekly_dashboard: "📈 Weekly dashboard: week {week} {year}"
    goals_update: "🎯 Goals update: metrics auto-update"
    validation: "✅ Validation: structure fix"
```

### Логика работы

1. **Получение изменений:**
   ```bash
   git status --porcelain
   ```

2. **Фильтрация:**
   Оставляет только файлы из `include_files`

3. **Определение типа коммита:**
   - Если есть `dashboards/weekly/` → `weekly_dashboard`
   - Если есть `dashboards/daily/` → `daily_dashboard`
   - Если есть `reflections/daily/` → `daily_reflection`
   - Если есть `goals/` → `goals_update`
   - Если есть `dashboards/validation/` → `validation`

4. **Создание коммита:**
   ```bash
   git add <filtered files>
   git commit -m "<message>"
   ```

5. **Опциональный push:**
   Если `auto_push: true`:
   ```bash
   git push origin main
   ```

### Безопасность

⚠️ **ВАЖНО:**
- `auto_push` по умолчанию **отключен**
- Включайте только для приватных репозиториев
- Используйте `.gitignore` для секретных данных

### Примеры commit messages

```
📝 Daily reflection: 2025-12-21
📊 Daily dashboard: 2025-12-21
📈 Weekly dashboard: week 51 2025
🎯 Goals update: metrics auto-update
✅ Validation: structure fix
```

### Примеры

```bash
# Создать автокоммит
python3 scripts/git_auto_commit.py

# Проверить что будет закоммичено
git status

# Просмотр истории автокоммитов
git log --grep="📝\|📊\|📈\|🎯\|✅" --oneline
```

---

## schedule_manager.py

### Назначение

Центральный скрипт для управления cron расписанием всех задач автоматизации.

### Использование

```bash
# Установить/обновить расписание
python3 scripts/schedule_manager.py setup

# Показать текущее расписание
python3 scripts/schedule_manager.py status

# Включить автоматизацию
python3 scripts/schedule_manager.py enable

# Отключить автоматизацию
python3 scripts/schedule_manager.py disable

# Показать все задачи
python3 scripts/schedule_manager.py list

# Запустить задачу вручную
python3 scripts/schedule_manager.py run <task_name>
```

### Команды

#### setup / enable

Создает или обновляет crontab с записями plan_expo.

**Процесс:**
1. Загрузить конфигурацию из `config/schedule.yaml`
2. Получить текущий crontab (`crontab -l`)
3. Удалить старые записи plan_expo
4. Генерировать новые записи
5. Установить обновленный crontab (`crontab <file>`)

**Маркеры в crontab:**
```cron
# BEGIN plan_expo automation
... записи ...
# END plan_expo automation
```

#### disable

Удаляет записи plan_expo из crontab, сохраняя другие записи.

#### status

Показывает:
- Статус (включена/отключена)
- Все запланированные задачи с расписанием
- Дату последней генерации

#### list

Показывает все доступные задачи:
- Ежедневные задачи
- Еженедельные задачи
- Проверки здоровья
- Группы задач

#### run <task>

Запускает задачу вручную для тестирования.

**Доступные задачи:**
- `morning_reflection`
- `analyze_reflection`
- `update_metrics`
- `track_streaks`
- `daily_dashboard`
- `git_commit`
- `notify_daily`
- `weekly_dashboard`
- `validate_structure`
- `notify_weekly`
- `health_check`

**Группы:**
- `daily` - запустить весь вечерний конвейер
- `weekly` - запустить все недельные задачи

### Конфигурация

Файл: `config/schedule.yaml`

Основные параметры:
```yaml
schedule:
  python_path: "python3"              # Путь к Python
  project_root: "/path/to/project"    # Путь к проекту

daily:
  morning_reflection:
    enabled: true
    time: "07:00"                     # Время запуска
    script: "scripts/generate_reflection.py"

  evening_pipeline:
    enabled: true
    tasks:
      - name: "analyze_reflection"
        time: "21:00"
        script: "scripts/analyze_reflection.py"
```

### Логи

Все задачи пишут логи в: `logs/cron/`

```
logs/cron/
├── morning_reflection.log
├── analyze_reflection.log
├── update_metrics.log
└── ...
```

### Примеры

```bash
# Первоначальная настройка
python3 scripts/schedule_manager.py setup

# Проверить что установлено
python3 scripts/schedule_manager.py status

# Тестовый запуск вечернего конвейера
python3 scripts/schedule_manager.py run daily

# Временно отключить на время отпуска
python3 scripts/schedule_manager.py disable

# Включить после возвращения
python3 scripts/schedule_manager.py enable

# Изменить расписание
# 1. Отредактировать config/schedule.yaml
# 2. Переустановить:
python3 scripts/schedule_manager.py setup
```

---

## notification_system.py

### Назначение

Отправка уведомлений через macOS Notification Center, Telegram, Slack.

### Использование

```bash
# Дневное уведомление
python3 scripts/notification_system.py daily

# Недельное уведомление
python3 scripts/notification_system.py weekly

# Проверка здоровья (warnings)
python3 scripts/notification_system.py check
```

### Параметры

```
NOTIFICATION_TYPE    Тип: daily, weekly, check
```

### Типы уведомлений

#### daily

Контекстное уведомление:

**Утро (до 12:00):**
```
plan_expo: Утренняя рефлексия
📝 Шаблон рефлексии создан для 2025-12-21. Время заполнить!
Звук: Glass
```

**Вечер (после 12:00):**
```
plan_expo: Дашборд готов
📊 Дневной дашборд создан. Операции: 85%, Тактика: 72%, Streak: 7
Звук: Hero
```

#### weekly

```
plan_expo: Недельный отчет
📈 Недельный дашборд готов. Средние показатели: 82% операций, 68% тактики
Звук: Hero
```

#### check

Проверяет условия и отправляет предупреждения:

1. **Незаполненная рефлексия:**
   Если текущий час ≥ `reflection_fill_deadline_hours` (по умолчанию 12)

2. **Streak под угрозой:**
   Если текущий streak < `streak_warning_threshold` (по умолчанию 3)

3. **Низкая производительность:**
   Проверяет последние N дней

**Пример warning:**
```
plan_expo: Предупреждение
⚠️ Рефлексия за сегодня еще не заполнена!
Звук: Basso
```

### Каналы уведомлений

#### macOS Notification Center

**Требования:**
- macOS
- Разрешения для Terminal/iTerm

**Технология:**
```bash
osascript -e 'display notification "message" with title "title" sound name "sound"'
```

**Доступные звуки:**
- default
- Glass
- Hero
- Basso
- Sosumi

#### Telegram

**Настройка:**

1. Создать бота через @BotFather
2. Получить bot_token
3. Узнать chat_id через @userinfobot
4. Обновить config/notifications.yaml:

```yaml
telegram:
  enabled: true
  bot_token: "123456:ABC-DEF1234ghIkl"
  chat_id: "987654321"
```

5. Установить requests:
```bash
pip3 install requests
```

**Формат:**
```
*plan_expo: Дашборд готов*

📊 Дневной дашборд создан...
```

Поддерживает Markdown форматирование.

#### Slack

**Настройка:**

1. Создать Incoming Webhook
2. Получить webhook URL
3. Обновить config/notifications.yaml:

```yaml
slack:
  enabled: true
  webhook_url: "https://hooks.slack.com/services/..."
```

4. Установить requests:
```bash
pip3 install requests
```

### Конфигурация

Файл: `config/notifications.yaml`

```yaml
notifications:
  enabled: true
  default_channels: ["macos"]  # macos, telegram, slack

macos:
  enabled: true
  sound: "default"

telegram:
  enabled: false
  bot_token: ""
  chat_id: ""

slack:
  enabled: false
  webhook_url: ""

templates:
  morning:
    title: "plan_expo: Утренняя рефлексия"
    message: "📝 Шаблон рефлексии создан для {date}"
    sound: "Glass"

health_checks:
  streak_warning_threshold: 3
  reflection_fill_deadline_hours: 12
  low_performance_threshold: 50
```

### Примеры

```bash
# Отправить дневное уведомление
python3 scripts/notification_system.py daily

# Отправить недельное уведомление
python3 scripts/notification_system.py weekly

# Проверить условия (запускается каждый час)
python3 scripts/notification_system.py check

# Тестирование всех каналов
# 1. Включить все каналы в config/notifications.yaml
# 2. Запустить:
python3 scripts/notification_system.py daily
```

---

## Общие паттерны

### Обработка ошибок

Все скрипты:
```python
try:
    main()
except KeyboardInterrupt:
    print("\n⚠️ Прервано пользователем")
    sys.exit(0)
except Exception as e:
    print(f"❌ Критическая ошибка: {e}", file=sys.stderr)
    import traceback
    traceback.print_exc()
    sys.exit(1)
```

### Exit коды

```
0  - Успех
1  - Ошибка
```

### Логирование

Формат:
```python
logging.basicConfig(
    filename=f'logs/{script_name}_{date}.log',
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)
```

### Переиспользование кода

```python
import sys
from pathlib import Path
sys.path.append(str(Path(__file__).parent))
from analyze_reflection import parse_reflection_file
from generate_reflection import get_active_goals
```

---

## Расширение функциональности

### Добавление новой задачи

1. Создать скрипт в `scripts/`
2. Добавить в `config/schedule.yaml`:

```yaml
daily:
  evening_pipeline:
    tasks:
      - name: "my_new_task"
        time: "21:30"
        script: "scripts/my_new_script.py"
        description: "My new automation"
```

3. Переустановить расписание:
```bash
python3 scripts/schedule_manager.py setup
```

### Добавление нового канала уведомлений

1. Расширить `notification_system.py`:

```python
def send_discord_notification(title, message, config):
    # Ваша реализация
    pass
```

2. Обновить `config/notifications.yaml`
3. Добавить в `send_notification()`

---

## Поддержка

Для решения проблем см. [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)
