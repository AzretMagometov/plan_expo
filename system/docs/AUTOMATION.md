# План экспо: Руководство по автоматизации

Полное руководство по системе автоматизации plan_expo - научно-обоснованной системе управления целями с трёхуровневой структурой.

## Содержание

1. [Обзор системы](#обзор-системы)
2. [Быстрый старт](#быстрый-старт)
3. [Архитектура автоматизации](#архитектура-автоматизации)
4. [Управление расписанием](#управление-расписанием)
5. [Конфигурация](#конфигурация)
6. [Интеграции](#интеграции)
7. [Мониторинг и логи](#мониторинг-и-логи)

---

## Обзор системы

План экспо автоматизирует полный цикл управления целями:

### Ежедневный поток (Daily Pipeline)

```
07:00 → Генерация шаблона рефлексии
        ↓
[Пользователь заполняет рефлексию]
        ↓
21:00 → Анализ рефлексии
21:05 → Обновление метрик целей
21:10 → Отслеживание streaks привычек
21:15 → Генерация HTML дашборда
21:20 → Git автокоммит
21:25 → Уведомление
```

### Еженедельный поток (Weekly Pipeline)

```
Воскресенье 22:00
        ↓
22:00 → Генерация недельного дашборда
22:05 → Валидация структуры целей
22:10 → Недельное уведомление
```

### Проверки здоровья (Health Checks)

```
Каждый час → Проверка условий:
  - Незаполненная рефлексия?
  - Streak под угрозой?
  - Низкая производительность?
```

---

## Быстрый старт

### 1. Установка расписания

```bash
# Перейти в директорию проекта
cd /Users/azretmagometov/Projects/plan_expo

# Просмотреть доступные задачи
python3 scripts/schedule_manager.py list

# Установить cron расписание
python3 scripts/schedule_manager.py setup

# Проверить статус
python3 scripts/schedule_manager.py status
```

### 2. Ручной запуск для тестирования

```bash
# Запустить вечерний конвейер
python3 scripts/schedule_manager.py run daily

# Запустить недельный конвейер
python3 scripts/schedule_manager.py run weekly

# Запустить проверку здоровья
python3 scripts/schedule_manager.py run health_check

# Запустить отдельную задачу
python3 scripts/schedule_manager.py run analyze_reflection
python3 scripts/schedule_manager.py run daily_dashboard
```

### 3. Проверка логов

```bash
# Просмотреть логи вечернего конвейера
tail -f logs/cron/analyze_reflection.log
tail -f logs/cron/daily_dashboard.log

# Просмотреть логи обновления метрик
tail -f logs/metrics_update_*.log

# Просмотреть все логи
ls -lh logs/cron/
```

---

## Архитектура автоматизации

### Компоненты системы

#### 1. validate_goals.py
**Назначение:** Валидация структуры целей и рефлексий

**Функции:**
- Проверка обязательных секций в файлах целей
- Валидация форматов метаданных
- Миграция файлов в правильную структуру
- Генерация отчетов валидации

**Использование:**
```bash
# Проверка без изменений
python3 scripts/validate_goals.py

# Автоматическое исправление
python3 scripts/validate_goals.py --fix

# Отчет: dashboards/validation/YYYY-MM-DD_validation_report.md
```

#### 2. auto_update_metrics.py
**Назначение:** Автоматическое обновление метрик в файлах целей

**Обновляет:**
- Identity Evidence List (количество доказательств)
- OKR/SMART Tracker (процент выполнения)
- Daily Habits Tracker (streaks, проценты)
- История изменений ([PROGRESS] записи)

**Использование:**
```bash
# Обновить за сегодня
python3 scripts/auto_update_metrics.py

# Обновить за конкретную дату
python3 scripts/auto_update_metrics.py --date 2025-12-21

# Обновить за неделю
python3 scripts/auto_update_metrics.py --period week
```

#### 3. track_habit_streaks.py
**Назначение:** Отслеживание непрерывных цепочек выполнения привычек

**Вычисляет:**
- Текущий streak для каждой привычки
- Максимальный streak
- Процент выполнения за 7/30 дней
- Паттерны по дням недели

**Выход:**
- JSON: `dashboards/streaks/streaks_data.json`
- Markdown: `dashboards/streaks/YYYY-MM-DD_streaks.md`

#### 4. generate_daily_dashboard.py
**Назначение:** Генерация интерактивного HTML дашборда

**Содержит:**
- Метрики: операции %, тактика %, доказательства, streak
- SVG график трендов за 7 дней
- Топ-5 привычек с текущими streaks
- Цветовая индикация (зеленый ≥80%, желтый 60-79%, красный <60%)

**Выход:**
- HTML: `dashboards/daily/YYYY-MM-DD.html`
- Markdown: `dashboards/daily/YYYY-MM-DD.md`

#### 5. generate_weekly_dashboard.py
**Назначение:** Недельная аналитика и сравнения

**Содержит:**
- Сравнение с предыдущей неделей (↑↓→)
- График выполнения по дням
- Топ-3 достижения
- Области для улучшения
- Автоматические инсайты

**Выход:**
- HTML: `dashboards/weekly/week_WW_YYYY.html`
- Markdown: `dashboards/weekly/week_WW_YYYY.md`

#### 6. git_auto_commit.py
**Назначение:** Автоматические Git коммиты

**Функции:**
- Фильтрация файлов (только reflections/, dashboards/, goals/)
- Формирование commit message по шаблонам
- Опциональный auto-push

**Шаблоны commit:**
```
📝 Daily reflection: 2025-12-21
📊 Daily dashboard: 2025-12-21
📈 Weekly dashboard: week 51 2025
🎯 Goals update: metrics auto-update
✅ Validation: structure fix
```

**Конфигурация:** `config/git_auto_commit.yaml`

#### 7. schedule_manager.py
**Назначение:** Центральное управление расписанием

**Команды:**
```bash
setup    # Создать/обновить crontab
status   # Показать текущее расписание
enable   # Включить автоматизацию
disable  # Отключить автоматизацию
list     # Показать все задачи
run      # Запустить задачу вручную
```

#### 8. notification_system.py
**Назначение:** Отправка уведомлений

**Типы:**
- `daily`: Утренние/вечерние уведомления
- `weekly`: Недельный отчет
- `check`: Предупреждения (незаполненная рефлексия, streak под угрозой)

**Каналы:**
- macOS Notification Center (встроенный)
- Telegram (опционально)
- Slack (опционально)

---

## Управление расписанием

### Просмотр расписания

```bash
python3 scripts/schedule_manager.py status
```

Пример вывода:
```
✅ Автоматизация plan_expo: ВКЛЮЧЕНА

Запланированные задачи:
--------------------------------------------------------------------------------
# Generated at 2025-12-21 15:00:00
# Project: /Users/azretmagometov/Projects/plan_expo

# Generate daily reflection template
0 7 * * * cd /Users/azretmagometov/Projects/plan_expo && python3 scripts/generate_reflection.py

# Analyze daily reflection
0 21 * * * cd /Users/azretmagometov/Projects/plan_expo && python3 scripts/analyze_reflection.py

...
--------------------------------------------------------------------------------
```

### Включение/отключение

```bash
# Включить автоматизацию
python3 scripts/schedule_manager.py enable

# Отключить автоматизацию
python3 scripts/schedule_manager.py disable
```

### Изменение расписания

Отредактируйте `config/schedule.yaml`:

```yaml
daily:
  morning_reflection:
    enabled: true
    time: "07:00"  # Измените время здесь

  evening_pipeline:
    enabled: true
    start_time: "21:00"  # Измените время начала конвейера
```

После изменения:
```bash
python3 scripts/schedule_manager.py setup
```

---

## Конфигурация

### config/schedule.yaml

Основная конфигурация расписания:

```yaml
schedule:
  enabled: true
  timezone: "Europe/Moscow"
  python_path: "python3"
  project_root: "/путь/к/проекту"

daily:
  morning_reflection:
    enabled: true
    time: "07:00"

  evening_pipeline:
    enabled: true
    tasks:
      - name: "analyze_reflection"
        time: "21:00"
      # ... другие задачи

weekly:
  enabled: true
  day: 0  # 0 = Воскресенье
  tasks:
    - name: "weekly_dashboard"
      time: "22:00"

health_checks:
  enabled: true
  interval: "hourly"
```

### config/notifications.yaml

Настройка уведомлений:

```yaml
notifications:
  enabled: true
  default_channels: ["macos"]  # macos, telegram, slack

macos:
  enabled: true
  sound: "default"

telegram:
  enabled: false
  bot_token: "YOUR_BOT_TOKEN"
  chat_id: "YOUR_CHAT_ID"

slack:
  enabled: false
  webhook_url: "YOUR_WEBHOOK_URL"

health_checks:
  streak_warning_threshold: 3
  reflection_fill_deadline_hours: 12
  low_performance_threshold: 50
```

### config/git_auto_commit.yaml

Настройка Git автокоммитов:

```yaml
git:
  auto_commit: true
  auto_push: false  # ВАЖНО: включайте только если уверены
  include_files:
    - "reflections/"
    - "dashboards/"
    - "goals/"

  templates:
    daily_reflection: "📝 Daily reflection: {date}"
    daily_dashboard: "📊 Daily dashboard: {date}"
    weekly_dashboard: "📈 Weekly dashboard: week {week} {year}"
```

---

## Интеграции

### macOS Notification Center

Встроенная интеграция. Работает сразу после установки.

**Настройка разрешений (если уведомления не приходят):**
1. Системные настройки → Уведомления
2. Найти Terminal (или iTerm)
3. Включить уведомления

### Telegram

1. Создать бота через @BotFather
2. Получить токен бота
3. Узнать свой chat_id через @userinfobot
4. Обновить `config/notifications.yaml`:

```yaml
telegram:
  enabled: true
  bot_token: "123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"
  chat_id: "987654321"
```

5. Установить requests:
```bash
pip3 install requests
```

### Slack

1. Создать Incoming Webhook в настройках Slack workspace
2. Скопировать Webhook URL
3. Обновить `config/notifications.yaml`:

```yaml
slack:
  enabled: true
  webhook_url: "https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXX"
```

4. Установить requests:
```bash
pip3 install requests
```

---

## Мониторинг и логи

### Структура логов

```
logs/
├── cron/                    # Логи cron задач
│   ├── morning_reflection.log
│   ├── analyze_reflection.log
│   ├── update_metrics.log
│   ├── track_streaks.log
│   ├── daily_dashboard.log
│   ├── git_commit.log
│   ├── notify_daily.log
│   ├── weekly_dashboard.log
│   ├── validate_structure.log
│   └── health_check.log
└── metrics_update_YYYY-MM-DD.log  # Детальные логи обновления метрик
```

### Просмотр логов

```bash
# Последние 50 строк лога
tail -50 logs/cron/daily_dashboard.log

# Отслеживание в реальном времени
tail -f logs/cron/analyze_reflection.log

# Поиск ошибок
grep "ERROR\|❌" logs/cron/*.log

# Просмотр всех логов за сегодня
grep "$(date +%Y-%m-%d)" logs/cron/*.log
```

### Очистка старых логов

```bash
# Удалить логи старше 30 дней
find logs/ -name "*.log" -mtime +30 -delete

# Архивировать логи
tar -czf logs_archive_$(date +%Y%m%d).tar.gz logs/
```

---

## Проверка работоспособности

### Ежедневная проверка

```bash
# 1. Проверить статус расписания
python3 scripts/schedule_manager.py status

# 2. Запустить тестовый конвейер
python3 scripts/schedule_manager.py run daily

# 3. Проверить логи
tail -20 logs/cron/daily_dashboard.log

# 4. Открыть дашборд
open dashboards/daily/$(date +%Y-%m-%d).html
```

### Еженедельная проверка

```bash
# 1. Проверить недельный дашборд
python3 scripts/schedule_manager.py run weekly

# 2. Проверить отчет валидации
cat dashboards/validation/*_validation_report.md

# 3. Проверить Git историю
git log --oneline -10
```

---

## Расширенные сценарии

### Запуск на другой машине

```bash
# 1. Клонировать репозиторий
git clone <repository_url>
cd plan_expo

# 2. Обновить пути в конфигурации
# Отредактировать config/schedule.yaml:
#   project_root: "/новый/путь/к/проекту"

# 3. Установить расписание
python3 scripts/schedule_manager.py setup
```

### Временное отключение

```bash
# Отключить на время отпуска
python3 scripts/schedule_manager.py disable

# Включить после возвращения
python3 scripts/schedule_manager.py enable
```

### Ручное восстановление метрик

```bash
# Обновить метрики за прошлую неделю
for i in {0..6}; do
  date=$(date -v-${i}d +%Y-%m-%d)
  python3 scripts/auto_update_metrics.py --date $date
done
```

---

## Безопасность

### Git автокоммиты

- `auto_push` по умолчанию **отключен**
- Включайте только если репозиторий приватный
- Используйте `.gitignore` для секретов

### Конфигурационные файлы

```bash
# Добавить в .gitignore
echo "config/notifications.yaml" >> .gitignore

# Создать шаблон без секретов
cp config/notifications.yaml config/notifications.yaml.example
# Удалить токены из .example файла
```

### Разрешения

```bash
# Сделать скрипты исполняемыми только для владельца
chmod 700 scripts/*.py

# Ограничить доступ к конфигурации
chmod 600 config/*.yaml
```

---

## Поддержка

### Проблемы?

См. [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) для решения распространенных проблем.

### Детальная документация скриптов

См. [SCRIPTS.md](./SCRIPTS.md) для подробного описания каждого скрипта.

### Обратная связь

Создайте issue в репозитории проекта с описанием проблемы или предложения.
