# Установка и настройка plan_expo

Подробное руководство по установке AI-системы целеполагания plan_expo.

## Содержание

- [Требования](#требования)
- [Быстрая установка](#быстрая-установка)
- [Ручная установка](#ручная-установка)
- [Настройка AI модели](#настройка-ai-модели)
- [Настройка уведомлений](#настройка-уведомлений)
- [Автоматизация](#автоматизация)
- [Решение проблем](#решение-проблем)

---

## Требования

### Обязательные
- **Python 3.8+** - для запуска скриптов
- **Git** - для клонирования репозитория
- **macOS / Linux / Windows (WSL)** - поддержка операционных систем

### Опциональные
- **AI модель** - Claude, GPT-4, Gemini или локальная модель
- **Telegram бот** - для push-уведомлений
- **Slack** - для командных уведомлений

---

## Быстрая установка

### macOS / Linux

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/yourusername/plan_expo.git
   cd plan_expo
   ```

2. **Запустите скрипт установки:**
   ```bash
   chmod +x setup.sh
   ./setup.sh
   ```

   Скрипт автоматически:
   - Проверит Python и pip
   - Установит зависимости
   - Запустит инициализацию с настройками по умолчанию

3. **Готово!** Переходите к [Настройке AI модели](#настройка-ai-модели)

### Windows (WSL)

```bash
# Установите WSL 2 если еще не установлен
wsl --install

# В WSL терминале:
git clone https://github.com/yourusername/plan_expo.git
cd plan_expo
./setup.sh
```

---

## Ручная установка

Если предпочитаете ручную настройку или скрипт setup.sh не работает:

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/plan_expo.git
cd plan_expo
```

### 2. Установка зависимостей

```bash
pip3 install -r requirements.txt
```

Устанавливается:
- `PyYAML` - для чтения конфигов
- `requests` - для Telegram/Slack уведомлений (опционально)
- `python-dotenv` - для управления переменными окружения

### 3. Инициализация проекта

Выберите один из трех режимов:

**Интерактивный (рекомендуется):**
```bash
python3 system/scripts/init_user.py
```
Задаст вопросы о часовом поясе, уведомлениях и AI модели.

**Быстрый:**
```bash
python3 system/scripts/init_user.py --mode quick
```
Использует настройки по умолчанию.

**Минимальный:**
```bash
python3 system/scripts/init_user.py --mode minimal
```
Только базовая настройка, без уведомлений и выбора AI.

### 4. Проверка установки

```bash
# Проверить что директории созданы
ls -la user_data/
ls -la system/

# Проверить что конфиги созданы
ls -la config/
```

---

## Настройка AI модели

plan_expo поддерживает несколько AI моделей. Выберите подходящую:

### Вариант 1: Claude (рекомендуется)

**Через Cursor / Claude Desktop:**
1. Откройте проект в Cursor или Claude Desktop
2. `.cursorrules.example` автоматически загрузится
3. Начните диалог: "Я хочу достичь [ваша цель]"

**Через API:**
```bash
# Добавьте в .env:
ANTHROPIC_API_KEY=your_api_key_here

# Получить API key: https://console.anthropic.com/
```

### Вариант 2: ChatGPT (GPT-4)

```bash
# Добавьте в .env:
OPENAI_API_KEY=your_api_key_here

# Получить API key: https://platform.openai.com/api-keys
```

### Вариант 3: Gemini (бесплатно)

```bash
# Добавьте в .env:
GOOGLE_AI_API_KEY=your_api_key_here

# Получить API key: https://makersuite.google.com/app/apikey
```

### Вариант 4: Локальная модель (приватность)

**Через Ollama:**
```bash
# Установить Ollama
brew install ollama  # macOS
# или скачать с https://ollama.ai

# Скачать модель
ollama pull mistral

# Использовать
ollama run mistral
```

**Через LMStudio:**
1. Скачать LMStudio: https://lmstudio.ai
2. Загрузить модель (Mistral, Llama 3, Phi-3)
3. Запустить локальный сервер

### Переключение между моделями

```bash
# Редактировать config/ai_model.yaml
nano config/ai_model.yaml

# Изменить current_model:
current_model: "claude"  # или "gpt", "gemini", "local"
```

Подробнее: [system/docs/AI_MODELS.md](system/docs/AI_MODELS.md)

---

## Настройка уведомлений

### macOS Notification Center (по умолчанию)

Работает из коробки. Никакой настройки не требуется.

Требования:
- Разрешите уведомления для Terminal/iTerm в System Settings

### Telegram (опционально)

1. **Создайте бота:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните `bot token`

2. **Получите Chat ID:**
   - Откройте [@userinfobot](https://t.me/userinfobot)
   - Отправьте `/start`
   - Сохраните ваш `chat_id`

3. **Добавьте в .env:**
   ```bash
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   TELEGRAM_CHAT_ID=123456789
   ```

4. **Включите в config/notifications.yaml:**
   ```yaml
   telegram:
     enabled: true
   ```

### Slack (опционально)

1. **Создайте Incoming Webhook:**
   - Перейдите: https://api.slack.com/messaging/webhooks
   - Выберите канал
   - Скопируйте Webhook URL

2. **Добавьте в .env:**
   ```bash
   SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX
   ```

3. **Включите в config/notifications.yaml:**
   ```yaml
   slack:
     enabled: true
   ```

---

## Автоматизация

### Настройка cron расписания

```bash
# Настроить автоматические задачи
python3 system/scripts/schedule_manager.py setup

# Проверить статус
python3 system/scripts/schedule_manager.py status

# Включить/выключить
python3 system/scripts/schedule_manager.py enable
python3 system/scripts/schedule_manager.py disable
```

### Что автоматизируется

**Ежедневно:**
- 07:00 - Генерация шаблона рефлексии
- 21:00 - Анализ рефлексии
- 21:05 - Обновление метрик целей
- 21:10 - Подсчет streaks привычек
- 21:15 - Генерация дневного дашборда
- 21:20 - Git auto-commit (если включено)
- 21:25 - Отправка уведомления

**Еженедельно (воскресенье):**
- 22:00 - Генерация недельного дашборда
- 22:05 - Валидация структуры целей
- 22:10 - Отправка недельного уведомления

**Ежечасно:**
- Health check (проверка незаполненных рефлексий, streaks под угрозой)

### Ручной запуск задач

```bash
# Запустить всю дневную цепочку
python3 system/scripts/schedule_manager.py run daily

# Запустить отдельную задачу
python3 system/scripts/schedule_manager.py run analyze_reflection
python3 system/scripts/schedule_manager.py run daily_dashboard
```

---

## Создание первой цели

После установки создайте свою первую цель:

1. **Откройте проект в Cursor / Claude Desktop**

2. **Обратитесь к AI-коучу:**
   ```
   Я хочу [описание вашей цели]
   ```

3. **AI проведет вас через процесс:**
   - Стратегический уровень (идентичность)
   - Тактический уровень (OKR или SMART)
   - Операционный уровень (If-Then планы, Tiny Habits)

4. **Файл цели будет создан в:**
   ```
   user_data/goals/goal_YYYY_MM_DD_[description].md
   ```

5. **Начните ежедневные рефлексии:**
   ```bash
   python3 system/scripts/generate_reflection.py
   ```

---

## Решение проблем

### Скрипты не запускаются

**Проблема:** `python3: command not found`

**Решение:**
```bash
# macOS
brew install python3

# Ubuntu/Debian
sudo apt install python3 python3-pip

# Windows (WSL)
sudo apt update && sudo apt install python3
```

---

### Импорт config_loader не работает

**Проблема:** `ModuleNotFoundError: No module named 'config_loader'`

**Решение:**
```bash
# Убедитесь что запускаете скрипты из корня проекта
cd /path/to/plan_expo
python3 system/scripts/generate_reflection.py

# Или добавьте system/scripts в PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:./system/scripts"
```

---

### Уведомления не приходят

**macOS:**
- Проверьте разрешения: System Settings → Notifications → Terminal/iTerm
- Включите "Allow Notifications"

**Telegram:**
- Проверьте .env файл
- Убедитесь что bot token и chat_id правильные
- Проверьте config/notifications.yaml:
  ```yaml
  telegram:
    enabled: true
  ```

**Slack:**
- Проверьте webhook URL в .env
- Убедитесь что webhook активен
- Проверьте права доступа для workspace

---

### Cron не запускается

**Проблема:** Задачи не выполняются автоматически

**Решение:**
```bash
# Проверить статус
python3 system/scripts/schedule_manager.py status

# Переустановить cron задачи
python3 system/scripts/schedule_manager.py setup

# Проверить crontab вручную
crontab -l

# Проверить логи
tail -f user_data/logs/cron/*.log
```

---

### Конфликт путей

**Проблема:** Скрипты не могут найти user_data/ или config/

**Решение:**
```bash
# Проверить структуру директорий
tree -L 2

# Должно быть:
# plan_expo/
# ├── system/
# ├── user_data/
# └── config/

# Запустить инициализацию заново
python3 system/scripts/init_user.py --mode quick
```

---

## Дополнительная документация

- **README.md** - Обзор системы, быстрый старт
- **CLAUDE.md** - Инструкции для AI-коуча
- **goal_setting_guide.md** - Научная база методов целеполагания
- **DATA_STRUCTURE.md** - Спецификация форматов данных
- **system/docs/AI_MODELS.md** - Детальное руководство по AI моделям
- **system/docs/SCRIPTS.md** - Документация по скриптам
- **system/docs/AUTOMATION.md** - Гайд по автоматизации
- **system/docs/TROUBLESHOOTING.md** - Решение проблем

---

## Поддержка

- **Issues:** [GitHub Issues](https://github.com/yourusername/plan_expo/issues)
- **Документация:** [Wiki](https://github.com/yourusername/plan_expo/wiki)
- **Обсуждения:** [GitHub Discussions](https://github.com/yourusername/plan_expo/discussions)

---

## Что дальше?

1. ✅ Установка завершена
2. ✅ AI модель настроена
3. ✅ Уведомления настроены (опционально)
4. ✅ Автоматизация настроена (опционально)

**Следующие шаги:**

- 📝 Создайте первую цель
- 🔄 Начните ежедневные рефлексии
- 📊 Отслеживайте прогресс через дашборды
- 🎯 Достигайте целей системно!

**Удачи в достижении целей!** 🚀
